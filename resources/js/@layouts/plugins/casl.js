/**
 * Stub — CASL removed. Auth will use Laravel Passport/Sanctum.
 * These functions are imported by @layouts nav components, so they must exist.
 */
export const can = () => true
export const canViewNavMenuGroup = item => item.children?.length > 0
export const canNavigate = () => true
